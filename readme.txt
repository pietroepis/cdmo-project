The following procedure to execute the scripts is valid for all the three tecniques (CP, SAT and LP)

In src/main.py, assign the name of the file to be executed to the filename variable, 
and decide whether you want to allow rotation or not by setting the variable allow_rotation.
Then, simply run main.py, staying in the same directory (i.e. src)